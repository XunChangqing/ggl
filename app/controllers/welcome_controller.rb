class WelcomeController < ApplicationController
  def index
    #@matches = Match.where(time: Time.now.midnight..Time.now.midnight+2.week)
    #Rails.logger.info "*************"
    #Rails.logger.info @matches.size
    #for d in 0..13
       #@tmatches = Match.where(time: Time.now.midnight+d.day..Time.now.midnight+(d+1).day)
       #Rails.logger.info @tmatches.size
    #end
  end
end
